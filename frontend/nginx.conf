server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: client-side routes like /my-bookings must resolve to
    # index.html on a hard refresh, not a 404 from nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
