// These are the seeded demo credentials from prisma/seed.ts, already
// published in the repo's README — safe to hardcode client-side because
// they're intentionally public. Never do this with a real secret.
export const DEMO_ADMIN_EMAIL = 'admin@example.com';
export const DEMO_ADMIN_PASSWORD = 'AdminPass123';
